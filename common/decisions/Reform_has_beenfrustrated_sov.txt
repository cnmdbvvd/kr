three_year_plan = { #三年计划

	Reform_has_beenfrustrated = { #改革受挫
		days_mission_timeout = 30 #定义变量

		selectable_mission = no # 如果设置，会将任务转换为玩家必须选择按钮的任务。默认为no
		icon = gfx_xx_oo #任务图标
		is_good = yes #yes为绿色；no为红色
		available = {
			always = no
	}
	timeout_effect = { #任务结束效果
		activate_mission = Reform_has_beenfrustrated #激活任务改革受挫
		add_days_mission_timeout = { #增加任务时间
			mission = three_year_plan #三年计划
			days = x #需要定义变量
		} #在国策定义变量后记得过来改
	}
	
}
}